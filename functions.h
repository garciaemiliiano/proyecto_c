#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void quitarDeLaLista(char nombre_producto) {
  // TODO: Funcion inversa
  // char filename[256] = "listado.txt";
  // FILE *file_buffer = fopen(filename, "r");
  // fprintf(file_buffer, "%s", nombre_producto);
  // printf("Fue agregado correctamente!\n");
}